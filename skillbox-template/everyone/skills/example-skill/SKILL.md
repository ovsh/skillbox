---
name: Example Skill
description: A starter skill showing the SKILL.md format — replace me with something useful
---

# Example Skill

This is a starter skill. A skill is a directory containing a `SKILL.md` with
YAML frontmatter (`name`, `description`) followed by instructions the agent
loads when the skill is invoked.

Delete this directory once your team has real skills.
