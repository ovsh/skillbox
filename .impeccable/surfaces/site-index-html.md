---
version: 1
slug: "site-index-html"
primary_target: "site/index.html"
related_targets: []
---

# Surface brief: site/index.html (Loadout landing page)

Mode: Persuade. Scope: single static page, source of truth `site/`, deployed
via personal-website repo at ovsh.github.io/loadout/.

Audience & job: developers running AI coding tools (Claude Code, Cursor,
OpenCode, agents dir) with dozens of skills accumulated on disk. They arrive
from GitHub or a link, deciding in seconds whether this menu-bar app is worth
installing. Gaming vocabulary lands with them ("loadout" needs no footnote).

Outcome: click Download (signed DMG, macOS 14+) or the GitHub/build-from-source
link.

Confirmed content spine (user-approved 2026-07-23):
1. What it is — "every skill your AI tools have, one menu-bar switch" + the
   loadout framing (the gear you take into a match).
2. See it — REAL screenshots only: actual menu bar item + library window.
3. How it works — three cards: sanctioned `skillOverrides` writes for Claude
   Code (live, folder never moves) · lossless per-tool shelving for
   Cursor/agents/OpenCode · global prompt editor (CLAUDE.md / AGENTS.md).
4. Trust — concrete safety posture (backs up settings.json once, owns exactly
   one key, everything reversible), MIT, open source.
5. Action — Download signed DMG + build from source.

Proof on hand: the real app (buildable, screenshottable), researched mechanism
truth, MIT license. No testimonials/user counts/benchmarks exist — never
invent. Until the GitHub release is published, the Download button uses a
marked placeholder URL with honest visible copy.

Boundaries: must NOT reuse sibling PowerWatch site's pixel-sky "Daylight"
look (no pixel-art sky world). No fake data in screenshots beyond authored
demo skills, labeled if mistakable. Unslop voice everywhere incl. <title>.
Mobile-responsive. Self-contained static page (no build step), assets local.

Unresolved: final DMG asset URL (wired after release exists).
